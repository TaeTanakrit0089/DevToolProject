services:
  web:
    build:
      context: devtool_system/
    ports:
      - "8083:8000"  # Exposing internal port 8000 for web container
    command: [ "sh", "./start.sh" ]
    volumes:
      - ./devtool_system:/app
    env_file:
      - .env
    depends_on:
      - postgres_db

  postgres_db:
    image: postgres:16
    volumes:
      - postgres_data:/var/lib/postgresql/data  # Persistent data storage
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql  # Mount your SQL dump
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}

volumes:
  postgres_data:
